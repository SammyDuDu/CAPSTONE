# Docker Environment Configuration for KoSPA
# Copy this to .env and modify as needed

# PostgreSQL Configuration
POSTGRES_USER=kospa
POSTGRES_PASSWORD=user_PASSWORD123
POSTGRES_DB=kospa_db

# Application will automatically use:
# DATABASE_URL=postgresql://kospa:kospa123@db:5432/kospa_db
